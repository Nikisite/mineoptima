<!DOCTYPE html>
<html lang="ru">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Последние покупки</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f6f8;
      margin: 0;
      padding: 0;
      color: #333;
    }

    header {
      display: flex;
      background: #2c3e50;
      color: #fff;
      padding: 1rem;
      align-items: center;
      justify-content: space-between;
    }

    h1 {
      margin: 0;
    }

    main {
      max-width: 900px;
      margin: 2rem auto;
      padding: 1rem;
    }

    .card {
      background: white;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      padding: 1.5rem;
      margin-bottom: 2rem;
    }

    form .form-group {
      margin-bottom: 1rem;
    }

    label {
      display: block;
      margin-bottom: 0.3rem;
      font-weight: bold;
    }

    input,
    select {
      width: 100%;
      padding: 0.5rem;
      border-radius: 4px;
      border: 1px solid #ccc;
    }

    button {
      background: #3498db;
      color: white;
      border: none;
      padding: 0.6rem 1.2rem;
      border-radius: 4px;
      cursor: pointer;
    }

    button:hover {
      background: #2980b9;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 1rem;
    }

    th,
    td {
      padding: 0.8rem;
      border-bottom: 1px solid #ddd;
    }

    th {
      background: #f0f0f0;
    }

    .badge {
      padding: 0.3rem 0.6rem;
      border-radius: 4px;
      font-size: 0.7rem;
      color: white;
      font-weight: bold;
    }

    .badge.success {
      background: #2ecc71;
    }

    .badge.progress {
      background: #f1c40f;
    }

    .badge.canceled {
      background: #e74c3c;
    }

    .delete-btn {
      background: #e74c3c;
      padding: 0.4rem 0.8rem;
      border-radius: 4px;
    }

    .delete-btn:hover {
      background: #c0392b;
    }
  </style>
</head>

<body>
  <header>
    <a style="text-decoration:none; color: #ddd;" href="/admin">
      <h2>⬅ Вернутся к редатированию</h2>
    </a>
    <p><a style="text-decoration:none; color: #ddd;" href="/admin/logout">🚪 Выйти</a></p>
  </header>

  <main>
    <div class="card">
      <h2 style="display: flex;justify-content: space-between;align-items: center;">Добавить покупку <b
          style="font-size: 0.8rem; color: gray;">[TEST]</b>
      </h2>
      <form method="POST" action="/admin/add-purchase">
        <div class="form-group">
          <label>Никнейм</label>
          <input type="text" name="username" required>
        </div>
        <div class="form-group">
          <label>Товар</label>
          <input type="text" name="item" required>
        </div>
        <div class="form-group">
          <label>Статус оплаты</label>
          <select name="status" required>
            <option value="success">✅ Успешная оплата</option>
            <option value="progress">⏳ В прогрессе</option>
            <option value="canceled">❌ Отменена</option>
          </select>
        </div>
        <button type="submit">Добавить</button>
      </form>
    </div>

    <!-- Таблица -->
    <div class="card">
      <h2>Список покупок</h2>
      <table>
        <thead>
          <tr>
            <th>Ник</th>
            <th>Товар</th>
            <th>Сервер</th>
            <th>Сумма</th>
            <th>Статус</th>
            <th>Дата</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% if (purchases.length===0) { %>
            <tr>
              <td colspan="7" style="text-align:center;">Нет покупок</td>
            </tr>
            <% } else { %>
              <% purchases.forEach(p=> { %>
                <tr>
                  <td>
                    <%= p.username %>
                  </td>
                  <td>
                    <%= p.item %>
                  </td>
                  <td>
                    <%= p.server %>
                  </td>
                  <td>
                    <%= p.amount %> ₽
                  </td>
                  <td>
                    <span class="badge <%= p.status %>">
                      <% if (p.status==='success' ) { %>✅ Успешная<% } %>
                          <% if (p.status==='progress' ) { %>⏳ В прогрессе<% } %>
                              <% if (p.status==='canceled' ) { %>❌ Отменена<% } %>
                    </span>
                  </td>
                  <td>
                    <%= new Date(p.date).toLocaleString() %>
                  </td>
                  <td>
                    <form method="POST" action="/admin/delete-purchase" style="display:inline;">
                      <input type="hidden" name="id" value="<%= p.id %>">
                      <button type="submit" class="delete-btn">Удалить</button>
                    </form>
                  </td>
                </tr>

                <% }) %>
                  <% } %>
        </tbody>
      </table>
    </div>
  </main>
</body>

</html>