<!DOCTYPE html>
<html lang="ru">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" /> <!-- адаптация под телефоны -->
  <title>Админка</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f7f9fc;
      color: #333;
      max-width: 900px;
      margin: 0 auto;
      padding: 0 15px 50px;
    }

    header {
      background-color: #0c4c80;
      padding: 15px;
      border-radius: 8px;
      color: #fff;
      text-align: center;
    }

    header h1 {
      margin: 0;
      font-weight: 700;
      font-size: 1.5em;
    }

    nav {
      margin-top: 15px;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 10px;
      align-content: center;
    }

    nav a {
      color: #e3f2fd;
      text-decoration: none;
      font-weight: 600;
      font-size: 1em;
      padding: 6px 10px;
      border-radius: 4px;
      background: rgba(255, 255, 255, 0.1);
      transition: background-color 0.25s ease;
    }

    nav a:hover {
      background-color: #1976d2;
      color: #fff;
    }

    nav a.logout {
      color: #ff7961;
      font-weight: 700;
      background: none;
    }

    nav a.logout:hover {
      background-color: #f44336;
      color: #fff;
    }

    h2,
    h3 {
      color: #444;
      margin-top: 30px;
    }

    form {
      margin: 10px 0;
    }

    input,
    select,
    button,
    textarea {
      margin: 5px 0;
      padding: 8px;
      width: 100%;
      max-width: 100%;
      box-sizing: border-box;
    }

    button.add {
      background: #4caf50;
      color: white;
      border: none;
      cursor: pointer;
    }

    button.delete {
      background: #f44336;
      color: white;
      border: none;
      cursor: pointer;
    }

    button.edit {
      background: #2196f3;
      color: white;
      border: none;
      cursor: pointer;
    }

    ul {
      list-style-type: none;
      padding: 0;
    }

    #donate-section ul li,
    #servers-section ul li {
      background: #fff;
      padding: 10px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 6px;
    }

    .item-header {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
      gap: 10px;
    }

    .donate-text,
    .server-text {
      flex-grow: 1;
      text-align: left;
    }

    .donate-text b,
    .server-text b {
      display: block;
      font-size: 1.1em;
    }

    img {
      max-width: 100%;
      height: auto;
      border-radius: 6px;
    }

    /* 📱 Адаптив для телефонов */
    @media (max-width: 600px) {
      header h1 {
        font-size: 1.2em;
      }

      nav {
        flex-direction: column;
        align-items: center;
      }

      nav a {
        width: 100%;
        text-align: center;
      }

      .item-header {
        flex-direction: column;
        align-items: stretch;
      }

      button {
        width: 100%;
      }
    }
  </style>
</head>

<body>

  <header>
    <h1>👑 Админка</h1>
    <nav>
      <a href="/">Главная</a>
      <a href="/admin/purchases">🛒 Покупки</a>
      <a href="#servers-section">🌐 Серверы</a>
      <a href="#donate-section-title">💎 Донаты</a>
      <a href="#background-section">🖼 Фон</a>
      <a href="/admin/edit-rules">📜 Правила</a>
      <a href="/admin/logout" class="logout">🚪 Выйти</a>
    </nav>
  </header>

  <!-- Серверы -->
  <h2>🌐 Серверы</h2>
  <div id="servers-section">
    <% if (Array.isArray(servers) && servers.length> 0) { %>
      <ul>
        <% servers.forEach(srv=> { %>
          <li>
            <div class="item-header">
              <div class="server-text">
                <b>
                  <%= srv.name %>
                </b>
                <p>
                  <%= srv.ip %>
                </p>
              </div>
              <div>
                <form method="POST" action="/admin/delete-server" style="display:inline;">
                  <input type="hidden" name="id" value="<%= srv.id %>" />
                  <button type="submit" class="delete">❌ Удалить</button>
                </form>
                <button class="edit" onclick="toggleEdit('server-<%= srv.id %>')">✏️ Редактировать</button>
              </div>
            </div>
            <div id="server-<%= srv.id %>" class="edit-form" style="display:none;">
              <form method="POST" action="/admin/edit-server">
                <input type="hidden" name="id" value="<%= srv.id %>" />
                <input name="name" value="<%= srv.name %>" placeholder="Имя сервера" required /><br />
                <input name="ip" value="<%= srv.ip %>" placeholder="IP сервера" required /><br />
                <input name="avatar" value="<%= srv.avatar %>" required /><br />
                <input name="rconHost" value="<%= srv.rconHost %>" placeholder="RCON Host" required /><br />
                <input name="rconPort" value="<%= srv.rconPort %>" type="number" placeholder="RCON Port"
                  required /><br />
                <input name="rconPassword" value="<%= srv.rconPassword %>" placeholder="RCON Пароль" required /><br />
                <button class="add" type="submit">💾 Сохранить</button>
              </form>
            </div>
          </li>
          <% }) %>
      </ul>
      <% } else { %>
        <p>Нет серверов</p>
        <% } %>

          <h3>➕ Добавить сервер</h3>
          <form method="POST" action="/admin/add-server">
            <input name="id" placeholder="ID сервера" required /><br />
            <input name="name" placeholder="Имя сервера" required /><br />
            <input name="ip" placeholder="IP сервера" required /><br />
            <input name="avatar" placeholder="URL изображения сервера" required /><br />
            <input name="rconHost" placeholder="RCON Host" required /><br />
            <input name="rconPort" placeholder="RCON Port" type="number" required /><br />
            <input name="rconPassword" placeholder="RCON Пароль" required /><br />
            <button class="add" type="submit">Добавить</button>
          </form>
  </div>

  <hr />

  <!-- Донаты -->
  <h2 id="donate-section-title">💎 Донаты</h2>
  <div id="donate-section">
    <% if (Object.keys(donateOptions).length> 0) { %>
      <% Object.keys(donateOptions).forEach(serverId=> { %>
        <h3>🌐 <%= (servers.find(s=> String(s.id) === String(serverId))?.name || serverId) %></h3>
        <% if (donateOptions[serverId].length> 0) { %>
          <ul>
            <% donateOptions[serverId].forEach(d=> { %>
              <li>
                <div class="item-header">
                  <div class="donate-text">
                    <b>
                      <%= d.name %>
                    </b> (<%= d.price %> руб.)
                      <p class="description">
                        <% if (d.desc) { %>
                          <%- d.desc.replace(/\//g, '<br>/' ) %>
                            <% } else { %>
                              -
                              <% } %>
                      </p>
                  </div>
                  <div>
                    <form method="POST" action="/admin/delete-donate" style="display:inline;">
                      <input type="hidden" name="serverId" value="<%= serverId %>">
                      <input type="hidden" name="id" value="<%= d.id %>" />
                      <button type="submit" class="delete">❌ Удалить</button>
                    </form>
                    <button class="edit" onclick="toggleEdit('edit-<%= serverId %>-<%= d.id %>')">✏️
                      Редактировать</button>
                  </div>
                </div>
                <div id="edit-<%= serverId %>-<%= d.id %>" class="edit-form" style="display:none;">
                  <form method="POST" action="/admin/edit-donate/<%= d.id %>">
                    <input type="hidden" name="serverId" value="<%= serverId %>">
                    <input type="hidden" name="id" value="<%= d.id %>" />
                    <input name="name" value="<%= d.name %>" placeholder="Название доната" required /><br />
                    <input name="price" value="<%= d.price %>" type="number" placeholder="Цена" required /><br />
                    <textarea name="desc" rows="5" cols="17"><%= d.desc %></textarea><br />
                    <input name="rconCommand" value="<%= d.rconCommand %>" placeholder="RCON команда" required /><br />
                    <button class="add" type="submit">💾 Сохранить</button>
                  </form>
                </div>
              </li>
              <% }) %>
          </ul>
          <% } else { %>
            <p>Нет донатов для этого сервера</p>
            <% } %>
              <% }) %>
                <% } else { %>
                  <p>Нет донатов</p>
                  <% } %>

                    <h3>➕ Добавить донат</h3>
                    <form method="POST" action="/admin/add-donate">
                      <select name="serverId" required>
                        <% servers.forEach(srv=> { %>
                          <option value="<%= srv.id %>">
                            <%= srv.name %>
                          </option>
                          <% }) %>
                      </select><br />
                      <input name="name" placeholder="Название доната" required /><br />
                      <input name="price" placeholder="Цена" type="number" required /><br />
                      <textarea name="desc" rows="5" cols="17" placeholder="Описание доната"></textarea><br />
                      <input name="rconCommand" placeholder="RCON команда (use {player})" required /><br />
                      <button class="add" type="submit">Добавить</button>
                    </form>

  </div>

  <hr />

  <h2 id="background-section">🖼 Фон</h2>
  <% if (background && background.image) { %>
    <p>Текущий фон:</p>
    <img src="<%= background.image %>" alt="Фон" style="max-width:300px;" /><br />
    <% } else { %>
      <p>Фон не установлен</p>
      <% } %>
        <form method="POST" action="/admin/set-background">
          <input name="image" value="<%= background.image || '' %>" placeholder="URL изображения" required /><br />
          <button class="add" type="submit">Сохранить фон</button>
        </form>

        <hr />

        <h2>📜 Управление правилами</h2>
        <a href="/admin/edit-rules" style="text-decoration: none;" class="add">✏️ Редактировать правила</a>

        <hr />

        <script>
          function toggleEdit(id) {
            const el = document.getElementById(id);
            if (el) el.style.display = el.style.display === 'none' ? 'block' : 'none';
          }
        </script>

        <p><a style="text-decoration:none;" href="/admin/logout">🚪 Выйти</a></p>
</body>

</html>